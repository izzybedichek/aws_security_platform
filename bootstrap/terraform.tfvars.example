# Copy to terraform.tfvars and fill in. Do NOT commit the real file.
aws_region        = "us-east-1"
github_owner      = "izzybedichek"
github_repo       = "aws_security_platform"
state_bucket_name = "sast-tfstate-CHANGE-ME-globally-unique"
# lock_table_name, deploy_role_name, allowed_subs, create_oidc_provider
# all have sensible defaults.
